# GOECO Scope

- Flutter client in `goeco_app` and related assets.
- Node.js backend in `backend` providing demo APIs.
- Documentation under `docs` and top-level `README.md`.

## Out of Scope
- Production infrastructure and deployment automation.
